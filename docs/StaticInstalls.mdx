### fd & bat
find & cat replacements
These use the 'alias' ice to set up the aliases 'find' and 'cat'
https://github.com/sharkdp/fd
https://github.com/sharkdp/bat

### eza -- ls replacement (https://github.com/eza-community/eza)
This is using my eza-releaser repo which creates a binary for [eza](https://github.com/kiliantyler/eza-releaser)
This avoids having to cargo build the app
It also sets the alias for ls to eza so that `command ls` still works as expected

### delta
git diffs but pretty (https://github.com/dandavison/delta)

### jq
json parser (https://github.com/jqlang/jq)

### yq
yaml parser (https://github.com/mikefarah/yq)

### kubectx & kubeens
easy switching of k8s environments (https://github.com/ahmetb/kubectx)

### rg
ripgrep, fast efficient grepping of files (https://github.com/BurntSushi/ripgrep)

### Zoxide
cd replacement (https://github.com/ajeetdsouza/zoxide)

### g
the go version manager (https://github.com/voidint/g)
Related g exports handled outside of .zshrc

### gh
Github cli

## Fluxcd (https://fluxcd.io)

### helm
Helm (using helm-releaser, see eza)

  id-as'app/k9s'       sbin'k9s' derailed/k9s \
  id-as'kubectl/blame' sbin'kubectl-blame' knight42/kubectl-blame \
  id-as'app/popeye'    sbin'popeye' derailed/popeye \
  id-as'app/tenv'      sbin'tenv' eval'tenv completion zsh' tofuutils/tenv
